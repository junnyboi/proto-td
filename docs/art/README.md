# Art reference set — TORCHLIGHT & STEEL

Canon art-style proposal: `~/Documents/MGS docs/Prototype TD/prototype-td-art-style-proposal.md`
(plan-of-record location). This directory holds the generated reference art it embeds:
per character `<id>_key.png` (+@3x) full-body key art, `<id>_iso.png` (+@4x) battle sprite,
`<id>.json` provenance, plus `terrain_plate.png`. Regenerate any asset with
`python3 tools/artgen/gen_<id>.py` — deterministic, byte-identical.
