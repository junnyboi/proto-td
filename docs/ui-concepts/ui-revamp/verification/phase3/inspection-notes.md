# Final Battle UI Matrix

Live, withdrawal, tutorial, and terminal states pass at `1280×720` and `720×1280`. The spell deck and command controls do not overlap, the HUD omits debug tick, every battle command is focusable, Recenter remains accessible, transient guidance clears deployment controls, and the withdrawal sheet renders above every battle overlay with safe Cancel focus and Confirm-only defeat mutation. Terminal state suppresses obsolete navigation and exposes the debrief handoff as the sole post-battle action.

The capture runtime logs passed the Godot error scan. Detailed corrective findings are in `../approved-phase0/inspection-notes.md`.
