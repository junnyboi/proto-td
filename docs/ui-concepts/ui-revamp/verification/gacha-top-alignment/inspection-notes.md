# Gacha Media Top-Alignment Verification

The `2048×825` live cinematic and reduced-motion static final plate both use a true top-aligned cover crop. The top row of the authored 16:9 frame remains visible, the character head and lunar focal point are preserved, and excess height is cropped only from the bottom. The Skip Reveal control remains unobstructed; the settled identity, stars, continuation hint, and Pull Again action remain contained in the wide viewport.

The `720×1000` live cinematic and static result plate also remain top anchored. The arch and character head stay fixed at the authored top edge while the lower frame absorbs the crop. Reveal controls and settled identity UI remain reachable and contained. No black bars, center-crop head loss, or vertical hover drift is visible.

Premium Resonance browse art passes at `2048×825` and `720×1000`. The 16:9 static backdrop is top-aligned with horizontal centering only; wide view crops excess height from the bottom, while portrait view crops excess width symmetrically. Header, telemetry, cards, scrolling, and the fixed Resonate action remain intact.
