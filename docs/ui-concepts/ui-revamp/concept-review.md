# GPT Image 2 Concept Review

## Company Command

The Company Command concept passes as a visual target. It creates a disciplined two-zone composition: a cinematic adult Lunaris commander occupies the left hero stage, while a dense but readable command deck on the right carries faction standards, campaign progress, the next operation, and the primary Mission Control action. The slim left rail offers persistent access to Vahalla, Recruitment, Training, Premium Recruit, Settings, and Exit without competing with the primary mission path. The top resource chips and cyan selected-state treatment are immediately legible. Production implementation should use the repository’s canonical hero backdrop rather than the generated identity variant, preserve all currently available/disabled operation semantics, and render every label natively for localization.

## Campaign Map

The Campaign Map concept passes as a structural target. It gives the isometric world map visual priority, keeps eight operation nodes readable, and distinguishes selected, connected, and locked states through cyan route energy and gold neutral framing. The fixed right dossier exposes mission title, objective, threat, rewards, Marks, and the entry action without covering the map. The left chapter rail and bottom back action preserve navigational context. Production implementation must use the existing authored stage map/resources and real campaign progression data rather than the generated scenery or placeholder reward values, but should reproduce the asymmetric rails, node-state hierarchy, and full-height dossier.

## Premium Recruit

The Premium Recruit concept passes as a high-level target for character appeal and information hierarchy. The three adult Lunaris heroes dominate the composition while the left rail keeps rate, pity, next guarantee, Marks, details, and recent outcomes visible. Single- and ten-pull actions are large and materially differentiated; the embedded confirmation preview demonstrates how deterministic cost, pity, and remaining balance should be presented before commitment. Production must use the canonical character sheets and current deterministic economy values. It must also preserve the existing duplicate-life model rather than the concept’s decorative “essence” placeholder.

## Vahalla Memorial

The Vahalla concept passes as a solemn character-forward memorial target. It establishes a functional three-column hierarchy: filtered fallen roster, selected identity portrait, and service/last-operation/lives ledger. The memorial tone is achieved through low-saturation lunar space, gold engraving, and restrained cyan memory light rather than gore or generic gravestones. Production must retain current roster filters and actual campaign identity data, avoid implying that stored premium lives resurrect the displayed fallen instance, and keep the return action continuously reachable.

## Battle HUD

The Battle HUD concept passes as a composition and density target. It preserves the playable isometric field as the largest uninterrupted region, assigns operator cards to a narrow left rail, places authoritative battle metrics in a compact top bar, and keeps spells in a bottom dock. Tutorial, navigation/facing, and operator detail overlays occupy different edges rather than stacking over the same battlefield space. Production must use current runtime/chibi assets and the existing deterministic battle model; the concept’s hero names, stats, and ability values are non-authoritative placeholders. On smaller screens the operator rail should become horizontal and secondary dossier detail should collapse behind an explicit control.

## Portrait Command and Dialog System

The portrait concept passes as the responsive shell target. It protects the adult hero stage in the upper portion and moves command content into a bottom-attached local scroll sheet with full-width touch targets. The confirmation sheet demonstrates the shared modal system: a bounded dark scrim, clipped ornamental frame, concise copy, and two high-contrast actions. Production should not display a deployment confirmation on Company Command unless an existing action requires it; the visual is specifically a reusable dialog grammar for promotion, pull, retry, and destructive navigation confirmations. At 720×1280, modal width must remain below the safe viewport and background controls must become inert while the sheet is open.

## Portrait Premium Recruit

The portrait Premium Recruit concept passes its responsive target. Character appeal remains dominant without pushing pity, Marks, or both pull actions outside the first viewport. The bottom confirmation sheet preserves the pre-action cost and separates cancel/confirm choices with clear material priority. Production should reduce the banner crop if any canonical face or weapon becomes obscured at 720×1280, keep Back reachable above the banner, and derive all rates/costs/guarantees from the live projection.

## Portrait Battle and Results

The portrait battle/result concept passes as a two-state target. The live battle state moves operators to a compact horizontal strip, keeps authoritative metrics at the top, preserves most of the battlefield, and pins spells to the lower edge. The victory state is a bottom-attached sheet with rewards and all three continuation actions visible. Production must not render both states simultaneously; the image documents their shared geometry. Existing retry/return/next behavior and actual reward payloads remain authoritative, and the result sheet must trap focus without consuming map gestures behind it.
