# Anima War Web Release Verification

**Status:** Verified checkpoint; public promotion pending WebDev Publish
**Canonical source:** `a26fa7f7d8277cb313e44f108b2e845ddb392bd8`
**Runtime source:** `ed77425` (byte-identical through `ab4e16f` and documentation-only `a26fa7f`)
**WebDev host checkpoint:** `86c8975c`
**Checkpoint URI:** `manus-webdev://86c8975c`

This is technical release evidence, not narrative authority. The sole binding lore remains [`NARRATIVE_CANON.md`](./NARRATIVE_CANON.md).

## Forward reconciliation

The final source history contains the complete Company Manus Anima War implementation and all compatible concurrent work. The latter includes eight core-resident GPT Image 2 non-grunt enemy sprites with deterministic procedural motion, Grunt as the sole frame-animated enemy, one deduplicated click sound per enabled button, Reduced-Motion-safe defeat ambience, eleven advanced-operator visual packs, audio fixes, and production documentation. `a26fa7f` changes only `todo.md` after the verified runtime source, so no redundant binary export was created.

The WebDev working history was advanced repeatedly while this release was being prepared. Each compatible checkpoint was merged forward. The final host preserves the newest loader, favicon, zero-chrome iframe, static-enemy architecture, click audio, defeat ambience, advanced operators, release records, and Anima War runtime; no shared history was rewritten.

## Exact managed runtime

| Artifact group | Count | Bytes | Verification |
|---|---:|---:|---|
| Blocking core | 1 | 202,817,120 | `/manus-storage/index-ed77425_94f67e65_37ed9fea.pck`; SHA-256 `94f67e6547f13890dcbe591d837d2084e3a74cb887cc7943cdf46d5290a8a356` |
| Advanced-operator packs | 11 | 309,356,348 | Exact lengths and SHA-256 values; class-scoped and mounted with replacement disabled |
| Premium Resonance films | 6 | 155,764,928 | Exact lengths and `video/ogg`; orientation-first prefetch preserved |
| Managed release total | 18 | 667,938,396 | Redirect-following status, length, and MIME probes pass |

The patched JavaScript loader, 39,514,754-byte Godot 4.7.2 WASM, splash, worklets, and six film mappings remain unchanged. The retired enemy-variants pack is absent because static enemy sprites are core-resident. Exactly eleven advanced-operator pack specifications follow the standalone `--` user-argument separator.

## Validation

Godot `4.7.2.stable.official.ed1daf0bf` import and bounded boot pass. The complete 73-test baseline, independent 68-regression/six-smoke gate, strict lore/localization audits, and final focused narrative, localization, Results, audio, input-feedback, static-enemy, battle-layout, and content-pack regressions pass with clean diagnostics.

The visual record includes 100 English/Simplified Chinese narrative captures at 150% text scale in landscape and portrait, six onboarding/input captures, the permanent nine-enemy harness, the full eleven-class advanced-operator matrix, and four terminal/Results defeat captures.

TypeScript and production build pass. Static guards confirm one core, eleven operator packs, six films, one standalone delimiter, no retired enemy pack, no stale core mapping, no retired Company name, and no conflict markers.

The combined core dismisses the loader and was observed loading from browser cache in 1,435 ms. No optional PCK or film loaded eagerly. Desktop iframe, inner viewport, and canvas measure `(0,0) 1280×1100` with zero border. Portrait frame, viewport, canvas buffer, and rendered canvas measure 390×844. Native Enter reaches Company Command, pointer input opens the Anima Archive, and the approved corruption, human-farm, robot-empire, and real-soul copy is present. The final console contains no application, audio, fetch, WebAssembly, WebGL, resource, digest, or pack-mount errors.

## Deployment state

Checkpoint `86c8975c` is complete and ready for WebDev Publish. This session exposes checkpoint creation but no direct Publish API, so public promotion and public-origin verification remain the explicit handoff. The existing public domain is `https://protohost-sqtjrsla.manus.space/`; it must not be described as carrying this checkpoint until Publish is completed and the exact core is verified there.
